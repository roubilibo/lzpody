# Legacy files

This directory preserves the former Python implementation and installer.

The active implementation is the Go/Bubble Tea application in the repository
root. These files are retained for historical reference and are not used by
the current launcher or installer.

